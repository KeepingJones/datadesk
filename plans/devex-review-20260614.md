# DX Plan Review: DataDesk
Generated by /plan-devex-review on 2026-06-14
Branch: master | Mode: DX POLISH

---

## Developer Persona Card

```
TARGET DEVELOPER PERSONA
========================
Who:       Quant developer who clones and tries to run it
Context:   Evaluating DataDesk as a reference implementation or career showcase signal
Tolerance: Medium — technically competent, will debug one error, abandons on second cryptic fail
Expects:   pip install + .env copy + one command → something works
```

---

## Developer Empathy Narrative (confirmed accurate by user)

**T+0:00** — I land on the README. Architecture table is impressive: 221k bars, 33k Trump posts, shadow-first OMS. Quickstart looks manageable. Five steps.

**T+0:30** — `pip install -e .[dev]` works (3 min). Open `.env.example` and see 10+ variables. Not sure which are required. I add FRED_API_KEY (free) and skip T212/Alpaca.

**T+1:00** — `python main.py` prints "History store is empty — run: python -m datadesk.history.migrate or python main.py backfill <tickers>". Dashboard never starts. Quickstart promised :8000.

**T+2:00** — `python main.py backfill AAPL MSFT`. Runs. I don't know how many tickers I need. 45 seconds later, done. Run `python main.py` again. Different error — not enough tickers.

**T+4:00** — Try `python main.py weekly-update`. 20+ minutes. No progress bar. Go make tea.

**T+25:00** — Returns. Dashboard is up but leaderboard is empty — haven't run `python main.py backtest` yet.

**Total: 30+ min TTHW. 3 error states. No demo path.**

---

## Competitive DX Benchmark

| Tool | TTHW | Notable DX Choice |
|------|------|-------------------|
| Backtrader | ~3 min | Bundles synthetic sample data; works without any external data source |
| Zipline-reloaded | ~10 min | `zipline ingest` bundles quandl data; explicit data bundle concept |
| QuantConnect LEAN | 15+ min local / instant cloud | Cloud playground available; local needs Docker |
| **DataDesk (before)** | **30+ min** | **No demo data, 3 error states, Windows-only quickstart** |
| **DataDesk (target)** | **~12 min** | **init-db + backfill --preset ai_semi + backtest → serve** |

Competitive tier target: **Competitive (2-15 min)**. Achievable with seed flow.

---

## Magical Moment Specification

**Moment:** Seeing the strategy leaderboard populated with real walk-forward results vs SPY
**Delivery vehicle:** `backfill --preset ai_semi` seed command (no API key, yfinance free tier)
**Implementation requirements:**
- `python main.py init-db` — creates schema in all .db files
- `python main.py backfill --preset ai_semi` — fetches 25 AI/Semi tickers, batched in groups of 5 with 2s delay to avoid yfinance rate limits
- `python main.py backtest` — runs momentum strategy, populates platform.db
- `python main.py serve` — ops console on :8000

Full seed flow TTHW: ~12 minutes (init: 5s, backfill: 5 min, backtest: 3-5 min, serve: instant).

---

## Developer Journey Map (post-review)

| Stage | Developer Does | Friction Points | Status |
|-------|---------------|-----------------|--------|
| 1. Discover | Lands on GitHub, reads README | No screenshot, CAGR buried below methodology | FIXED (screenshot added) |
| 2. Install | `pip install -e .[dev]` | Doesn't know Python 3.11+ is required | FIXED (note added) |
| 3. Hello World | `python main.py backfill --preset ai_semi && python main.py backtest && python main.py serve` | Windows-only activation; unclear required keys | FIXED (multi-platform, labels) |
| 4. Real Usage | Runs rebalancer, OMS, daemons | OPERATIONS.md guides this (pending T7 from CEO plan) | PENDING |
| 5. Debug | Checks logs, shadow store | Error messages improved; error_log.txt exists | IMPROVED |
| 6. Upgrade | Updates dependencies | No CHANGELOG | FIXED (CHANGELOG added) |

---

## All Decisions

| # | Pass | Decision | Status |
|---|------|----------|--------|
| 1 | P1 | Add `backfill --preset ai_semi` (yfinance, 25 tickers, batched with rate-limit delay) | ACCEPTED |
| 2 | P1 | Fix quickstart: multi-platform activation + Python 3.11+ note | ACCEPTED |
| 3 | P1 | Add REQUIRED / OPTIONAL labels to .env.example | ACCEPTED |
| 4 | P2 | Change `python main.py` default to: show help + seed instructions | ACCEPTED |
| 5 | P2 | Add help strings to all argparse subcommands | ACCEPTED |
| 6 | P3 | Improve "History store is empty" error → point to `backfill --preset ai_semi` | ACCEPTED |
| 7 | P4 | Add dashboard screenshot to README | ACCEPTED |
| 8 | P4 | Add `pytest` to Quickstart | ACCEPTED |
| 9 | P5 | Create minimal CHANGELOG.md | ACCEPTED |
| 10 | P6 | Document launch.bat / run.ps1 in README | ACCEPTED |
| 11 | P7 | Add MIT LICENSE | ACCEPTED |
| 12 | P8 | Enable GitHub Discussions post-publish | ACCEPTED |
| 13 | OV | Add `python main.py init-db` as step 1 of quickstart (schema creation before backfill) | ACCEPTED |
| 14 | OV | Seed flow is: init-db + backfill preset + backtest → serve | ACCEPTED |
| 15 | OV | yfinance rate limit: batch in groups of 5 with 2s delay | ACCEPTED |

---

## Implementation Tasks

```
DX IMPLEMENTATION TASKS
========================

[ ] T1 (P1, CC: ~20 min) — main.py — Add `init-db` subcommand
    Creates schema in all .db files (history.db, platform.db, altdata.db, shadow.db).
    Add `CREATE TABLE IF NOT EXISTS` guards to all schema init functions.
    Surfaced by: Outside Voice — schema init is unaddressed on fresh clone
    Files: main.py, datadesk/history/store.py, datadesk/db.py, datadesk/live/shadow.py
    Verify: fresh clone → `python main.py init-db` → no errors

[ ] T2 (P1, CC: ~30 min) — main.py — Add `backfill --preset ai_semi`
    Fetch 25 AI/Semi tickers via yfinance (no API key). Batch in groups of 5 with 2s delay.
    On failure: clear error with "Try re-running — yfinance may be rate-limiting".
    Surfaced by: Pass 1 — TTHW 30+ min; Outside Voice — yfinance rate limits
    Files: main.py, datadesk/ingest/backfill.py (or equivalent)
    Verify: fresh clone → `python main.py backfill --preset ai_semi` → completes in ~5 min

[ ] T3 (P1, CC: ~10 min) — README.md — Fix multi-platform quickstart
    Add: `source .venv/bin/activate  # Mac/Linux` and `.venv\Scripts\activate  # Windows`
    Add: `Python 3.11+` requirement note at top of Quickstart
    Add: `pytest` line at end of Quickstart
    Add: document launch.bat / run.ps1 in Quickstart
    Surfaced by: Pass 1, 4, 6 — Windows-only, Python version missing, launchers undocumented
    Files: README.md
    Verify: README.md Quickstart shows both activation forms

[ ] T4 (P1, CC: ~10 min) — .env.example — Add REQUIRED / OPTIONAL labels
    Mark FRED_API_KEY as REQUIRED (free at fred.stlouisfed.org).
    Mark T212_*, ALPACA_*, TIINGO_*, OLLAMA_* as OPTIONAL.
    Note: DATADESK_ARM_BROKER defaults to 0 (safe).
    Surfaced by: Pass 1 — 10+ variables, unclear which are mandatory
    Files: .env.example
    Verify: .env.example has clear REQUIRED/OPTIONAL sections

[ ] T5 (P2, CC: ~30 min) — main.py — Add help strings to all subcommands + change default
    Change default (no-args) to print first-time guide with seed instructions.
    Add `help=` strings to all add_parser() calls.
    Surfaced by: Pass 2 — `--help` shows no descriptions; default launches 5-min backtest
    Files: main.py
    Verify: `python main.py --help` shows descriptions for all subcommands

[ ] T6 (P3, CC: ~10 min) — main.py — Improve "History store is empty" error
    Replace current message with:
    "History store empty. Seed it first:
      python main.py init-db
      python main.py backfill --preset ai_semi   # 25-ticker AI/Semi universe (~5 min)
    Then run: python main.py backtest && python main.py serve"
    Surfaced by: Pass 3 — two confusing alternatives, no guidance
    Files: main.py (cmd_backtest)
    Verify: run `python main.py backtest` with empty DB → improved message

[ ] T7 (P4, CC: ~15 min) — README + docs/assets/ — Add dashboard screenshot
    Capture screenshot of live ops console (Command & Control + Strategy P&L populated).
    Embed in README after architecture tree.
    Surfaced by: Pass 4 — no visual proof of working system
    Files: README.md, docs/assets/dashboard.png
    Verify: README shows screenshot above-the-fold in first viewport

[ ] T8 (P5, CC: ~10 min) — CHANGELOG.md — Create minimal changelog
    One section: ## [Unreleased] with the current set of code fixes and DX improvements.
    Surfaced by: Pass 5 — no changelog signals immaturity to evaluators
    Files: CHANGELOG.md (new)

[ ] T9 (P7, CC: ~5 min) — LICENSE — Add MIT license
    Standard MIT license.
    Surfaced by: Pass 7 — no license prevents legal use/reference by evaluators
    Files: LICENSE (new)

[ ] T10 (P8, ~0 min) — GitHub — Enable Discussions post-publish
    Enable in repo Settings after making public.
    Surfaced by: Pass 8 — no feedback mechanism
```

---

## NOT in Scope

- Makefile / Taskfile — Outside voice suggested this; low value for quant audience vs Python CLI
- CHANGELOG full history — Unreleased section is sufficient for v0.1.0
- TypeScript types — Not a JS/TS project
- Plugin/extension ecosystem — Solo project
- CONTRIBUTING.md — Not seeking contributions

---

## What Already Exists

- `pyproject.toml` — pytest, ruff configured; `pip install -e .[dev]` installs everything
- `tests/` — 10 existing tests, pytest configured
- `docs/backtesting.md` + `docs/ui_documentation.md` — linked from README
- `.env.example` — Already committed, safe to share
- `datadesk/ingest/` — yfinance backfill already exists (just needs preset flag)
- Error logging — `error_log.txt` in project root

---

## DX Scorecard

```
+====================================================================+
|              DX PLAN REVIEW — SCORECARD                             |
+====================================================================+
| Dimension            | Score  | Prior  | Trend  |
|----------------------|--------|--------|--------|
| Getting Started      |  8/10  |  3/10  |  +5 ↑  |
| API/CLI/SDK          |  8/10  |  6/10  |  +2 ↑  |
| Error Messages       |  8/10  |  5/10  |  +3 ↑  |
| Documentation        |  8/10  |  6/10  |  +2 ↑  |
| Upgrade Path         |  8/10  |  6/10  |  +2 ↑  |
| Dev Environment      |  7/10  |  6/10  |  +1 ↑  |
| Community            |  7/10  |  4/10  |  +3 ↑  |
| DX Measurement       |  6/10  |  4/10  |  +2 ↑  |
+--------------------------------------------------------------------+
| TTHW                 | ~12min | 30+min |    ↑   |
| Competitive Rank     | Competitive (target met)                    |
| Magical Moment       | Designed — backfill preset → serve          |
| Product Type         | CLI Tool + Platform                         |
| Mode                 | DX POLISH                                   |
| Overall DX           |  7.5/10|  5/10  |  +2.5↑ |
+====================================================================+
| DX PRINCIPLE COVERAGE                                               |
| Zero Friction      | covered (init-db + preset seed)               |
| Learn by Doing     | covered (backfill → backtest → serve flow)    |
| Fight Uncertainty  | covered (improved error messages)             |
| Opinionated + Escape Hatches | covered (preset with explicit alt) |
| Code in Context    | covered (screenshot, real data in demo)       |
| Magical Moments    | covered (leaderboard with real walk-forward)  |
+====================================================================+
```

---

## DX Implementation Checklist

```
DX IMPLEMENTATION CHECKLIST
============================
[ ] TTHW < 15 min (target: ~12 min with seed flow)
[ ] python main.py init-db creates all schemas
[ ] python main.py backfill --preset ai_semi seeds 25 tickers
[ ] python main.py with no args shows help + seed instructions
[ ] All subcommands have help strings
[ ] "History store empty" error points to seed flow
[ ] Dashboard screenshot in README
[ ] Quickstart is multi-platform (Mac/Linux + Windows)
[ ] .env.example labels REQUIRED vs OPTIONAL
[ ] pytest mentioned in Quickstart
[ ] CHANGELOG.md exists
[ ] MIT LICENSE exists
[ ] launch.bat / run.ps1 documented in README
[ ] GitHub Discussions enabled (post-publish)
```

---

## TODOS.md Updates

**DX-TODO-1 (deferred):** Makefile / task runner (`make seed`, `make serve`)  
Currently: `python main.py` commands. For a quant audience, Python CLI is sufficient. Revisit if project gains contributors.

**DX-TODO-2 (deferred):** Progress bar for backfill preset  
Currently: no progress indicator during 5-min seed. TQDM or simple counter would help.  
Depends on: T2 (backfill preset) being shipped.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAN | 4 proposals, 3 accepted, 1 deferred; 4 code bugs found and added to plan |
| Outside Voice | Claude subagent + Gemini | Independent 2nd opinion | 2 | CLEAN | 8 issues from subagent (4 fixed in plan, 4 already handled); 5 from Gemini (2 real, 3 already mitigated) |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEARED | 5 issues (E1-E5): E1 OPERATIONS.md restart claim corrected; E3-E5 Fix B/D architecture clarified; E2 non-blocking |
| Design Review | `/design-review` | UI/UX gaps | 1 | CLEARED | 8 findings: 7 fixed (buttons, danger styling, headings, P&L scroll, Qty format, sign bug, focus rings); 1 deferred (FINDING-008 serif font). Score C+ → B-. |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | CLEARED | Score 5/10 → 7.5/10. TTHW 30+ min → ~12 min. 15 decisions: init-db command, backfill preset with rate-limit batching, multi-platform quickstart, REQUIRED/OPTIONAL .env labels, better defaults, help strings, improved errors, dashboard screenshot, CHANGELOG, LICENSE. |

**VERDICT:** CEO + ENG + DESIGN + DX CLEARED — all review gates passed.

NO UNRESOLVED DECISIONS
